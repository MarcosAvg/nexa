# Production Database Repair

Date: 2026-08-19
Project: `nexa`

## Applied Migrations

- `20260819195144_repair_security_and_indexes`
- `20260819195208_make_dashboard_metrics_invoker`

## Changes

- Enabled `security_invoker` on `cards_ordered` and `personnel_with_status`.
- Removed current table and sequence privileges from `anon` in `public`.
- Restricted RPC execution to authenticated users where needed.
- Removed API execution privileges from the trigger-only user profile function.
- Fixed function `search_path` configuration.
- Changed fuzzy personnel search and dashboard metrics to `SECURITY INVOKER`.
- Restricted responsiva deletion to administrators.
- Restricted enlace writes to administrators and operators while preserving reads.
- Added indexes for the previously unindexed foreign keys.
- Revoked anonymous default privileges for future public tables, sequences, and functions.

## Verification

Post-repair counts remained unchanged:

- `personnel`: 2,846
- `cards`: 5,219
- `signed_responsivas`: 885
- `tickets`: 36
- `history_logs`: 12,381

The security advisor no longer reports the previous security-definer views,
anonymous RPC execution, or authenticated dashboard security-definer RPCs.

Remaining advisor items are warnings about extensions installed in `public`
and leaked-password protection, plus informational unused-index reports. The
new indexes are expected to initially appear as unused and should be reviewed
after normal application traffic.

No application data, tables, or columns were deleted or updated.
